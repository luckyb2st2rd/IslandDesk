[package]
name = "islanddesk-platform-windows"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
islanddesk-core = { path = "../core" }

[target.'cfg(windows)'.dependencies]
keyring = "4.2.0"
windows = { version = "0.62.2", features = [
  "Foundation",
  "Media_Control",
  "Storage_Streams",
  "Win32_Foundation",
  "Win32_Graphics_Gdi",
  "Win32_System_DataExchange",
  "Win32_System_Memory",
  "Win32_System_WinRT",
  "Win32_UI_WindowsAndMessaging",
] }

[lints.rust]
unsafe_code = "allow"
